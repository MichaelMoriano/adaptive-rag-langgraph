[project]
name = "adaptive-rag-ui"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "chainlit>=1.0.0",
    "httpx>=0.27.0",
    "python-dotenv>=1.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"